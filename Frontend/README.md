# Munchman Frontend

A Next.js frontend for Munchman.

## Run locally

```bash
npm install
npm run dev
```

Open `http://localhost:3000` in your browser.

## Notes

- Built with Next.js
- App source lives in `src/`
- Start from `src/app/page.js`
